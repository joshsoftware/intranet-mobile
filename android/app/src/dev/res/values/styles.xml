<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.MaterialComponents.Light.NoActionBar.Bridge">
        <!-- Customize your theme here. -->
        <item name="android:editTextBackground">@drawable/rn_edit_text_material</item>
        <item name="colorPrimary">#FFFFFF</item>
        <item name="colorPrimaryDark">#FFFFFF</item>
        <item name="colorAccent">#FFFFFF</item>
    </style>

    <!-- BootTheme should inherit from a Bridge theme to support both Splash and AppCompat -->
    <style name="BootTheme" parent="Theme.MaterialComponents.Light.NoActionBar.Bridge">
        <item name="android:windowSplashScreenBackground">@color/bootsplash_background</item>
        <item name="android:windowSplashScreenAnimatedIcon">@mipmap/bootsplash_logo</item>
        <item name="android:windowIsTranslucent">true</item>
        <item name="postSplashScreenTheme">@style/AppTheme</item>
    </style>

</resources>
